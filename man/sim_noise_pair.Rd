% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_noise.R
\name{sim_noise_pair}
\alias{sim_noise_pair}
\title{Add calibrated ARMA noise to a pair of trend series}
\usage{
sim_noise_pair(
  trends,
  h,
  lambda_target = 1,
  scale = 1,
  ar.coefs = numeric(0),
  ma.coefs = numeric(0),
  order = NULL,
  s = NULL,
  n_start = 100,
  seed = NULL
)
}
\arguments{
\item{trends}{List returned by \code{\link[=sim_trends]{sim_trends()}}, with elements \code{x1} and \code{x2}.}

\item{h}{Integer. MA window for SNR calibration (passed to \code{\link[=sim_noise]{sim_noise()}}).}

\item{lambda_target}{Numeric scalar or length-2 vector. Target SNR for each
series. If scalar, the same target is used for both (default 1).}

\item{scale}{Numeric. Multiplicative scale applied to both trend series
before adding noise (default 1).}

\item{ar.coefs}{Numeric vector. AR coefficients (see \code{\link[=sim_noise]{sim_noise()}}).}

\item{ma.coefs}{Numeric vector. MA coefficients (see \code{\link[=sim_noise]{sim_noise()}}).}

\item{order}{\code{c(p, q)} integer vector, or a list of two such vectors. If
supplied, auto-generates ARMA coefficients independently for each series
(see \code{\link[=sim_noise]{sim_noise()}}). Cannot be combined with \code{ar.coefs} / \code{ma.coefs}.}

\item{s}{Integer or NULL. Window for local variance estimation (see
\code{\link[=sim_noise]{sim_noise()}}).}

\item{n_start}{Integer. Burn-in observations for \code{arima.sim} (default 100).}

\item{seed}{Integer or NULL. RNG seed for reproducibility. Seeds for the two
series are derived as \code{seed} and \code{seed + 1} so their noise processes are
independent.}
}
\value{
A list:
\describe{
\item{y1, y2}{Observed series (signal + noise), as plain numeric
vectors.}
\item{x1, x2}{Scaled trend series (i.e. \code{scale * trends$x1/x2}).}
\item{noise}{List with \code{series1} and \code{series2}, each containing:
\code{ar}, \code{ma} (coefficients used), \code{sigma} (innovation SD), \code{mean_snr}
(mean local SNR), and \code{noise_var_ratio} (empirical / theoretical
smooth noise variance; should be near 1).}
}
}
\description{
Convenience wrapper around \code{\link[=sim_noise]{sim_noise()}} for use with output from
\code{\link[=sim_trends]{sim_trends()}}. Applies noise models to both series and returns the
observed series alongside high-level diagnostics.
}
\details{
\code{lambda_target} and \code{order} can be supplied as a single value (shared across
both series) or as a length-2 vector / list (one per series). Seeds for the
two calls are derived independently from \code{seed} so that the two noise
processes are not identical.
}
\seealso{
\code{\link[=sim_noise]{sim_noise()}}, \code{\link[=sim_trends]{sim_trends()}}
}
