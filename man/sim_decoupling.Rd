% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{sim_decoupling}
\alias{sim_decoupling}
\title{Simulated coupled and decoupled series pairs}
\format{
A data frame with 1000 rows (two scenarios of 500 time points) and
6 variables:
\describe{
\item{scenario}{Factor: \code{"coupled"} or \code{"decoupled"}.}
\item{t}{Integer time index, 1 to 500.}
\item{y1, y2}{Numeric. Observed series (trend plus AR(1) noise).}
\item{nu1, nu2}{Numeric. True underlying trends (identical in the
coupled scenario).}
}
}
\usage{
sim_decoupling
}
\description{
Two simulated bivariate time series scenarios sharing the same base trends
and noise process, differing only in the L2 trend separation \eqn{d}:
\code{"coupled"} (\eqn{d = 0}; the trends are identical, so the local-similarity
null hypothesis holds everywhere) and \code{"decoupled"} (\eqn{d = 1.5};
structured separation concentrated in local episodes via a smooth coupling
weight).
}
\details{
Generated with \code{\link[=sim_trends]{sim_trends()}} (\code{method = "smooth"}, \code{bw = 50}, seed 101)
and \code{\link[=sim_noise_pair]{sim_noise_pair()}} with AR(1) noise (\eqn{\phi = 0.5}) calibrated to a
signal-to-noise ratio of 1.5 at smoothing bandwidth \eqn{h = 5}. The
generating script is \code{data-raw/sim_decoupling.R} in the package source
repository.
}
\examples{
cpl <- subset(sim_decoupling, scenario == "coupled")
plot(cpl$t, cpl$y1, type = "l", col = "grey60")
lines(cpl$t, cpl$nu1, lwd = 2)
}
\seealso{
\code{\link[=lomad]{lomad()}}, \code{\link[=sim_trends]{sim_trends()}}, \code{\link[=sim_noise_pair]{sim_noise_pair()}}
}
\keyword{datasets}
