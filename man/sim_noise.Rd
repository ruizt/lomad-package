% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_noise.R
\name{sim_noise}
\alias{sim_noise}
\title{Generate ARMA noise calibrated to a target signal-to-noise ratio}
\usage{
sim_noise(
  x.state,
  h,
  ar.coefs = numeric(0),
  ma.coefs = numeric(0),
  order = NULL,
  lambda_target = 1,
  s = NULL,
  n_start = 100,
  seed = NULL
)
}
\arguments{
\item{x.state}{Numeric vector. The signal (trend) to add noise to.}

\item{h}{Integer. Moving-average window length used for SNR calibration.}

\item{ar.coefs}{Numeric vector. AR coefficients of the noise process.
Defaults to \code{numeric(0)} (no AR component). Must yield a causal process
(all roots of the AR polynomial outside the unit circle). Cannot be used
together with \code{order}.}

\item{ma.coefs}{Numeric vector. MA coefficients of the noise process.
Defaults to \code{numeric(0)} (no MA component). Must yield an invertible
process (all roots of the MA polynomial outside the unit circle). Cannot
be used together with \code{order}.}

\item{order}{Integer vector of length 2, \code{c(p, q)}. If supplied, AR and MA
coefficients are auto-generated from a causal/invertible PACF
parameterisation. Cannot be used together with explicit \code{ar.coefs} or
\code{ma.coefs}.}

\item{lambda_target}{Numeric. Target signal-to-noise ratio (default 1).}

\item{s}{Integer or NULL. Window length for local variance estimation during
calibration. If \code{NULL}, defaults to \code{2 * h}.}

\item{n_start}{Integer. Number of burn-in observations for \code{arima.sim}
(default 100).}

\item{seed}{Integer or NULL. RNG seed for reproducibility. Applies to both
coefficient auto-generation (if \code{order} is used) and noise simulation.}
}
\value{
A list with three sublists:
\describe{
\item{inputs}{\code{ar}, \code{ma}, \code{lambda_target}, \code{seed}.}
\item{outputs}{\code{x} (signal), \code{noise}, \code{y} (observed = signal + noise),
all as plain numeric vectors.}
\item{diagnostics}{\code{sigma} (calibrated innovation SD),
\code{noise_var_ratio} (empirical/theoretical smooth noise variance; should
be near 1), \code{snr_local} (local SNR over the interior),
\code{lv_signal}, \code{lv_noise} (local variances of smoothed signal and
noise).}
}
}
\description{
Generates an ARMA noise series and adds it to a signal \code{x.state}, scaling
the innovation standard deviation so that the ratio of the smoothed signal
variance to the smoothed noise variance matches \code{lambda_target}.
}
\details{
Coefficients can be supplied directly via \code{ar.coefs} and \code{ma.coefs}, or
auto-generated by supplying \code{order = c(p, q)}. Auto-generation uses the
PACF parameterisation: partial autocorrelations are drawn uniformly from
(-0.95, 0.95) and converted to coefficients via the Durbin-Levinson
recursion, which guarantees causality and invertibility by construction.
}
\seealso{
\code{\link[=sim_noise_pair]{sim_noise_pair()}}, \code{\link[=sim_trends]{sim_trends()}}
}
