% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lomad_test_identity.R
\name{lomad_test_identity}
\alias{lomad_test_identity}
\title{Naive pointwise test of exact trend identity}
\usage{
lomad_test_identity(
  x1,
  x2,
  q,
  max_pq = 3L,
  alpha = 0.05,
  q_se = 10L * q,
  noise_override = NULL
)
}
\arguments{
\item{x1}{Numeric vector. First observed series.}

\item{x2}{Numeric vector. Second observed series.}

\item{q}{Integer. Moving-average window width for trend smoothing.}

\item{max_pq}{Integer. Maximum AR and MA order for AIC selection (default 3).}

\item{alpha}{Numeric. Significance level for BY correction (default 0.05).}

\item{q_se}{Integer. Pilot MA window for detrending the difference series
before ARMA fitting. Must satisfy \code{q_se > q}. Defaults to \code{10L * q}.}

\item{noise_override}{Optional list with elements \code{ar}, \code{ma} (optional),
\code{sigma2}. If supplied, these parameters are used to compute the variance
of the smoothed difference directly, bypassing ARMA estimation. The
parameters describe one noise series; the difference of two independent
copies has twice the variance, which is handled internally. Useful for
oracle experiments where the true noise process is known.}
}
\value{
A list with:
\describe{
\item{I}{Integer vector. 1 where adjusted p-value < alpha, 0 elsewhere,
NA at edges.}
\item{p_raw}{Numeric vector. Raw two-sided p-values.}
\item{p_adj}{Numeric vector. BY-adjusted p-values.}
\item{D}{Numeric vector. Pointwise difference of smoothed series.}
\item{se_D}{Numeric scalar. Standard error of D_t.}
\item{m_eff}{Integer. Effective number of independent tests.}
\item{fit_diff}{List. ARMA fit for the pilot-detrended difference
(NULL when \code{noise_override} is used).}
}
}
\description{
Tests \eqn{H_0: \nu_{1t} = \nu_{2t}} at each time point using the
pointwise difference in smoothed moving averages. Included as a comparison
method: because it tests exact identity rather than similarity, it rejects
whenever the trends differ by any nonzero amount.
}
